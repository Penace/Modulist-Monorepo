{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongoose": "^8.14.1",
    "morgan": "^1.10.0"
  },
  "scripts": {
    "dev-server": "npx json-server --watch db.json --port 3000 --host 0.0.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
